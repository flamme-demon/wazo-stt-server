# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# ASR engine
# Use onnx-asr without [cpu] extra to allow older onnxruntime
onnx-asr[hub]>=0.6.1
# Pin onnxruntime < 1.17 to avoid external data path validation issue
onnxruntime<1.17

# Audio processing
pydub>=0.25.1

# HTTP client
httpx>=0.26.0

# Database
aiosqlite>=0.19.0

# Utilities
python-dotenv>=1.0.0

# Speaker diarization (for call recordings)
pyannote.audio>=3.1.0
