services:
  wazo-stt-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: wazo-stt-server:latest
    container_name: wazo-stt-server
    ports:
      - "8000:8000"
    volumes:
      - data:/data
      - model-cache:/root/.cache
    environment:
      - MODEL_NAME=nemo-parakeet-tdt-0.6b-v3
      - DB_PATH=/data/transcriptions.db
      - HOST=0.0.0.0
      - PORT=8000
      - MAX_QUEUE_SIZE=100
      - MAX_AUDIO_DURATION_SECS=480
      - TEXT_RETENTION_DAYS=365
      - ENABLE_DIARIZATION=true
      # HuggingFace token for pyannote diarization (required for gated model)
      # Get your token at https://huggingface.co/settings/tokens
      # and accept conditions at https://huggingface.co/pyannote/speaker-diarization-3.1
      # - HF_TOKEN=hf_your_token_here
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

volumes:
  data:
    name: wazo-stt-data
  model-cache:
    name: wazo-stt-model-cache
